---
trigger: always_on
---

UGOKU Lab とは「うごくものづくりをもっと手軽に」をコンセプトとしたオープンソースのプロジェクトです。
# プロジェクト概要
## UGOKU Pad
ESP32などのマイコンとスマートフォンをBluetooth (BLE) で接続し、ジョイスティックやボタンなどのウィジェットを組み合わせて自作の操作パネルを作成できるアプリ。
## UGOKU One
「うごくものづくり」入門者向けのオールインワン汎用基板。
- **構成**: ESP32-WROOM-32E、単四電池4本駆動。
- **内蔵**: DCモータドライバ (TB67H450FNG) x2、IMU (BMI270)。
# ガイドライン
## 想定ユーザー層
Arduinoを少し触ったことがある初心者（中学生から大人）をメインとしています。
## 執筆・回答の方針
1. **平易な日本語**: 専門用語を多用せず、直感的に理解できる表現を心がけてください。
3. **コードコメント**: コード内のコメントは、処理の内容が初心者に伝わるよう丁寧な日本語で記述してください。

## チャットルール
- 日本語で応答してください。
- WEB初心者にもわかりやすい説明をしてください。

## デザインガイドライン
- PC,スマホどちらでも見やすいスタイルにしてください。
- 画像のサイズは比率で指定するようにしてください。
- UIは既存のライブラリを活用したモダンなデザインにしてください。

## コーディング規約 (CSS/HTML)
- README.mdのルールに準拠してコーディングしてください
- **CSS変数の使用**: 色や余白は `styles/main.css` で定義された変数 (`var(--primary-color)` など) を使用してください。ハードコードは避けてください。
- **ユーティリティクラス**: レイアウトには `.display-flex`, `.flex-center`, `.flex-between`, `.gap-medium`, `.gap-large` などのユーティリティクラスを優先して使用してください。
- **インラインスタイルの禁止**: HTMLタグに直接 `style="..."` を書かないでください。
- **既存クラスの再利用**: 新しいクラスを作る前に、既存の `.btn`, `.card`, `.alert-box` などが使えないか確認してください。
- **セマンティックHTML**: 適切なHTMLタグ（例: `<header>`, `<nav>`, `<main>`, `<section>`, `<article>`, `<footer>`）を使用して、コンテンツの意味を明確にしてください。

## 内容について
- 情報は正確にしてください。特にハードウェアの仕様についてわからないことがあれば勝手な創作はさけ、聞いてください。